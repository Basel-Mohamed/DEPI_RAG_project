global:
  scrape_interval: 15s

scrape_configs:
  - job_name: rag-api
    metrics_path: /metrics/prometheus
    scheme: http
    authorization:
      type: Bearer
      credentials: test_api_key
    static_configs:
      - targets:
          - host.docker.internal:8010
